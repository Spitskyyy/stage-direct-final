{% extends 'base.html.twig' %}

{% block title %}Nouvel Stage
{% endblock %}

{% block body %}
<body class="bg-gray-100 light:bg-gray-900">
<br>
<div class="flex items-center justify-center min-h-screen bg-gray-100 light:bg-gray-900 ">
	<div class="w-full max-w-md p-6 bg-white border border-gray-200 rounded-lg shadow sm:p-8 dark:bg-gray-800 dark:border-gray-700">
		<h5 class="text-xl font-medium text-gray-900 dark:text-white text-center mb-4">Créer un nouveau stage</h5>

		{{ form_start(form, {'attr': {'class': 'space-y-6'}}) }}

		<div>
			<label for="{{ form.title.vars.id }}" class="block mb-2 text-sm font-medium text-gray-900 dark:text-white">
				{{ form.title.vars.label|default('Titre du stage') }}
			</label>
			{{ form_widget(form.title, {'attr': {
                    'class': 'bg-gray-50 border border-gray-300 text-gray-900 text-sm rounded-lg focus:ring-blue-500 focus:border-blue-500 block w-full p-2.5 dark:bg-gray-600 dark:border-gray-500 dark:placeholder-gray-400 dark:text-white'
                }}) }}
			{{ form_errors(form.title) }}
		</div>


		<div>
			<label for="{{ form.start_date.vars.id }}" class="block mb-2 text-sm font-medium text-gray-900 dark:text-white">
				{{ form.start_date.vars.label|default('Date de début') }}
			</label>
			{{ form_widget(form.start_date, {'attr': {
                    'class': 'bg-gray-50 border border-gray-300 text-gray-900 text-sm rounded-lg focus:ring-blue-500 focus:border-blue-500 block w-full p-2.5 dark:bg-gray-600 dark:border-gray-500 dark:placeholder-gray-400 dark:text-white',
                    'type': 'date'
                }}) }}
			{{ form_errors(form.start_date) }}
		</div>

		<div>
			<label for="{{ form.end_date.vars.id }}" class="block mb-2 text-sm font-medium text-gray-900 dark:text-white">
				{{ form.end_date.vars.label|default('Date de fin') }}
			</label>
			{{ form_widget(form.end_date, {'attr': {
                    'class': 'bg-gray-50 border border-gray-300 text-gray-900 text-sm rounded-lg focus:ring-blue-500 focus:border-blue-500 block w-full p-2.5 dark:bg-gray-600 dark:border-gray-500 dark:placeholder-gray-400 dark:text-white',
                    'type': 'date'
                }}) }}
			{{ form_errors(form.end_date) }}
		</div>

		<div>
			<label for="{{ form.intern.vars.id }}" class="block mb-2 text-sm font-medium text-gray-900 dark:text-white">
				{{ form.intern.vars.label|default('Stagiaire') }}
			</label>
			{{ form_widget(form.intern, {'attr': {
                    'class': 'select-field',
                    'class': 'bg-gray-50 border border-gray-300 text-gray-900 text-sm rounded-lg focus:ring-blue-500 focus:border-blue-500 block w-full p-2.5 dark:bg-gray-600 dark:border-gray-500 dark:placeholder-gray-400 dark:text-white'

                }}) }}
			{{ form_errors(form.intern) }}
		</div>

		<div>
			<label for="{{ form.school.vars.id }}" class="block mb-2 text-sm font-medium text-gray-900 dark:text-white">
				{{ form.school.vars.label|default('École') }}
			</label>
			{{ form_widget(form.school, {'attr': {
                    'class': 'select-field',
                    'class': 'bg-gray-50 border border-gray-300 text-gray-900 text-sm rounded-lg focus:ring-blue-500 focus:border-blue-500 block w-full p-2.5 dark:bg-gray-600 dark:border-gray-500 dark:placeholder-gray-400 dark:text-white'

                }}) }}
			{{ form_errors(form.school) }}
		</div>

		<div>
			<label for="{{ form.company.vars.id }}" class="block mb-2 text-sm font-medium text-gray-900 dark:text-white">
				{{ form.company.vars.label|default('Entreprise') }}
			</label>
			{{ form_widget(form.company, {'attr': {
                    'class': 'select-field',
                    'class': 'bg-gray-50 border border-gray-300 text-gray-900 text-sm rounded-lg focus:ring-blue-500 focus:border-blue-500 block w-full p-2.5 dark:bg-gray-600 dark:border-gray-500 dark:placeholder-gray-400 dark:text-white'

                }}) }}
			{{ form_errors(form.company) }}
		</div>

		<div>
			<label for="{{ form.visitreport.vars.id }}" class="block mb-2 text-sm font-medium text-gray-900 dark:text-white">
				{{ form.visitreport.vars.label|default('Rapport de visite') }}
			</label>
			{{ form_widget(form.visitreport, {'attr': {
                    'class': 'select-field',
                    'class': 'bg-gray-50 border border-gray-300 text-gray-900 text-sm rounded-lg focus:ring-blue-500 focus:border-blue-500 block w-full p-2.5 dark:bg-gray-600 dark:border-gray-500 dark:placeholder-gray-400 dark:text-white'

                }}) }}
			{{ form_errors(form.visitreport) }}
		</div>

		<div>
			<label for="{{ form.activitylist.vars.id }}" class="block mb-2 text-sm font-medium text-gray-900 dark:text-white">
				{{ form.activitylist.vars.label|default('Liste d\'activités') }}
                </label>
                {{ form_widget(form.activitylist, {'attr': {
                    'class': 'select-field',
                    'class': 'bg-gray-50 border border-gray-300 text-gray-900 text-sm rounded-lg focus:ring-blue-500 focus:border-blue-500 block w-full p-2.5 dark:bg-gray-600 dark:border-gray-500 dark:placeholder-gray-400 dark:text-white'

                }}) }}
                {{ form_errors(form.activitylist) }}
            </div>

            		<div>
			<label for="{{ form.is_verified.vars.id }}" class="block mb-2 text-sm font-medium text-gray-900 dark:text-white">
				{{ form.is_verified.vars.label|default('Stage vérifié ?') }}
			</label>
			{{ form_widget(form.is_verified, {'attr': {
                    'class': 'checkbox-field',
                    'class': 'w-6 h-6 appearance-none border border-gray-300 rounded-md checked:bg-blue-500 checked:border-blue-500 focus:ring-2 focus:ring-blue-400 cursor-pointer dark:bg-gray-600 dark:border-gray-500 dark:checked:bg-blue-600 dark:focus:ring-blue-500'

                }}) }}
			{{ form_errors(form.is_verified) }}
		</div>

            <button type="submit" 
                class="w-full text-white bg-blue-700 hover:bg-blue-800 focus:ring-4 focus:outline-none focus:ring-blue-300 font-medium rounded-lg text-sm px-5 py-2.5 text-center dark:bg-blue-600 dark:hover:bg-blue-700 dark:focus:ring-blue-800">
                Créer le stage
            </button>

            <div class="text-sm font-medium text-gray-500 dark:text-gray-300 text-center">
                <a href="{{ path('app_internship_index') }}" class="text-blue-700 hover:underline dark:text-blue-500">
                    Retour à la liste
                </a>
            </div>
        {{ form_end(form) }}
    </div>
</div>

<style>
    .input-field {
        @apply bg-gray-50 border border-gray-300 text-gray-900 text-sm rounded-lg focus:ring-blue-500 focus:border-blue-500 block w-full p-2.5 dark:bg-gray-600 dark:border-gray-500 dark:placeholder-gray-400 dark:text-white;
    }

    .select-field {
        @apply bg-gray-50 border border-gray-300 text-gray-900 text-sm rounded-lg focus:ring-blue-500 focus:border-blue-500 block w-full p-2.5 dark:bg-gray-600 dark:border-gray-500 dark:placeholder-gray-400 dark:text-white;
    }

    .checkbox-field {
        @apply w-6 h-6 appearance-none border border-gray-300 rounded-md checked:bg-blue-500 checked:border-blue-500 focus:ring-2 focus:ring-blue-400 cursor-pointer dark:bg-gray-600 dark:border-gray-500 dark:checked:bg-blue-600 dark:focus:ring-blue-500;
    }
</style>
<body>
{% endblock %}
