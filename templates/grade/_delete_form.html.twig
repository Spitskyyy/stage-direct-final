<form method="post" action="{{ path('app_grade_delete', {'id': grade.id}) }}" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer cette classe : {{ grade.name }} ?');">
	<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ grade.id) }}">

	<button class="font-medium text-blue-600 dark:text-red-500 hover:underline">Delete</button></div>

</form>