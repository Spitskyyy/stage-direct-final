<form method="post" action="{{ path('app_school_delete', {'id': school.id}) }}" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer cette école : {{ school.name }} ?');">
	<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ school.id) }}">

	<button class="font-medium text-blue-600 dark:text-red-500 hover:underline">Delete</button></div>

</form>