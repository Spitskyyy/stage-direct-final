<form method="post" action="{{ path('app_speciality_delete', {'id': speciality.id}) }}" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer cette spécialité: {{ speciality.name }} ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ speciality.id) }}">

    <button class="font-medium text-blue-600 dark:text-red-500 hover:underline">Delete</button></div>

</form>