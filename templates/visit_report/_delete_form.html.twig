<form method="post" action="{{ path('app_visit_report_delete', {'id': visit_report.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ visit_report.id) }}">
    <button class="btn">Delete</button>
</form>
